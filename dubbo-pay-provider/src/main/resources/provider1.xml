<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://dubbo.apache.org/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans        http://www.springframework.org/schema/beans/spring-beans-4.3.xsd        http://dubbo.apache.org/schema/dubbo        http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="dubbo-pay-provider"/>

    <!-- 推荐使用zk来做注册发现.所以此时协议头改为zookeeper:// -->
    <dubbo:registry address="zookeeper://127.0.0.1:2181"/>
    <!-- 下面这种写法也可以 -->
    <!-- <dubbo:registry protocol="zookeeper" address="127.0.0.1:2181"/>-->

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880"/>

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="course.dubbo.pay.api.service.PayService" ref="payService" timeout="2000" retries="5"/>

    <!-- 和本地bean一样实现服务 -->
    <bean id="payService" class="course.dubbo.pay.provider.service.PayServiceImpl"/>
</beans>